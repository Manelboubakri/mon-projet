from setuptools import setup, find_packages

setup(
    name='mon-projet',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Ajouter ici toutes les dépendances nécessaires, par exemple :
        'langraph',  # Si 'langraph' est une dépendance
        'numpy',     # Exemples de dépendances
    ],
)
set
